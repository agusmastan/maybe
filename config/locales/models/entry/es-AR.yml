---
es-AR:
  activerecord:
    errors:
      models:
        entry:
          attributes:
            base:
              invalid_sell_quantity: no se pueden vender %{sell_qty} acciones de %{ticker} porque solo tenés %{current_qty} acciones
